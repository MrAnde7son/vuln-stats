version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - vuln_data:/data
    environment:
      - DB_PATH=/data/vuln_stats.db
    restart: unless-stopped

volumes:
  vuln_data:
